---
import config from '../../data/config.json';

interface Props {
  showImage?: boolean;
}

const { showImage = true } = Astro.props;
---

<section class="section">
  <div class="container">
    <div class="grid gap-12 lg:grid-cols-2 items-center">
      {showImage && (
        <div class="relative">
          <img
            src="/profile-placeholder.svg"
            alt="Galabau Fortkamp"
            class="rounded-xl shadow-lg w-full h-auto bg-neutral-100"
            loading="lazy"
          />
        </div>
      )}

      <div>
        <h2 class="section-heading">
	          {config.company.name} in {config.contact.address.city}
        </h2>
        <p class="text-lg text-neutral-600 mb-6 leading-relaxed">
		          Seit 3 Jahren arbeiten wir in {config.contact.address.city} und Umgebung im Garten und Landschaftsbau. Wir übernehmen Pflege, Arbeiten im Außenbereich und den Winterdienst. Vor dem Start besprechen wir, was gemacht wird und wie der Ablauf aussieht.
        </p>
        <p class="text-neutral-600 mb-8 leading-relaxed">
		          Wir sind ein kleines Team und packen auf der Baustelle selbst mit an. Wir kommen vorbei, schauen uns die Fläche an und erklären, wie wir vorgehen würden. Wir arbeiten für Privatgärten, Gewerbeflächen und Wohnanlagen. Dabei geht es oft um saubere Kanten, stabile Wege und eine Pflege, die zum Grundstück passt.
        </p>

	        <div class="grid grid-cols-2 gap-6 mb-8">
	          <div>
	            <h3 class="font-semibold text-neutral-900 mb-1">Absprachen</h3>
	            <p class="text-sm text-neutral-600">Termin und Ablauf werden vorher geklärt</p>
	          </div>

	          <div>
	            <h3 class="font-semibold text-neutral-900 mb-1">Lokal</h3>
		            <p class="text-sm text-neutral-600">Wir arbeiten in Ahaus und Umgebung</p>
	          </div>

	          <div>
	            <h3 class="font-semibold text-neutral-900 mb-1">Klare Preise</h3>
	            <p class="text-sm text-neutral-600">Sie bekommen ein Angebot, bevor es losgeht</p>
	          </div>

	          <div>
	            <h3 class="font-semibold text-neutral-900 mb-1">Direkt</h3>
	            <p class="text-sm text-neutral-600">Kurze Wege, direkte Rückmeldung</p>
	          </div>
        </div>

        <a href="/kontakt/" class="btn-primary">
          Kontakt aufnehmen
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>
